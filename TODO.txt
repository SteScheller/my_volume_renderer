bugfixes:
- flickering when watching the volume from below

refactoring:
- make volume data a class of its own and encapsulate the void pointer stuff
 and free memory automatically with destructor
    - see if binning (histograms) can also be added to that class

new features:
- panning of the volume in the output window
- orthographic projection option
- screenshot functionality
- size of rendered volume image independent of window size (allow for high
resolution screenshots)

